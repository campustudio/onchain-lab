# Optional. Used from slice 02 (Read) onward.
# A public testnet JSON-RPC endpoint. Leave unset to use the built-in default /
# simulated adapter. NEVER put a mainnet key, private key, or secret here.
#
# Example (Sepolia public RPC):
# VITE_TESTNET_RPC_URL=https://ethereum-sepolia-rpc.publicnode.com
